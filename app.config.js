const path = require('path');
const process = require('process');

const root = process.cwd();

module.exports = {
  pagePath: './client/pages',
  distDir: './dist',
  // pageExtensions: ['mdx', 'md', 'jsx', 'js', 'tsx', 'ts'],
}